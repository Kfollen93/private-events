<section class="section">
  <div class="container">
    <h1 class="newevent">
      New Event
    </h1>
    <p class="NewEvent">
    <div class="tile is-child box">
    <p class="userpage">
  <div class="container">
      <%= simple_form_for @event do |f| %>
      <%= f.input :title %>
      <%= f.input :body %>
      <%= f.input :date%>
      <%= f.button :submit %>
  <% end %>
  <%= yield %>
    </p>
    </p>
    </div>
  </div>
</section>